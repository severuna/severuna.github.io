import React from 'react';
import style from '../module/Page.module.css';

const Job = () => {
    return (
        <div className={`${style.content} column`}>
            <h1>Job</h1>
        </div>
    );
};

export default Job;